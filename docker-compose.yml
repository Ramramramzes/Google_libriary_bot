services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/data
      - ./myKey.json:/app/myKey.json:ro
    environment:
      - DB_PATH=/data/bot.db

volumes:
  bot_data:
